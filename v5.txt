<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ABC Store</title>

<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">

<style>
body{margin:0;font-family:'DM Sans',sans-serif;background:#f6f6f6}

/* TOP BAR */
.topbar{
background:#111;color:#fff;font-size:13px;padding:6px;
white-space:nowrap;overflow:hidden;text-align:center
}
.topbar span{display:inline-block;animation:scroll 15s linear infinite}
@keyframes scroll{
0%{transform:translateX(100%)}
100%{transform:translateX(-100%)}
}

/* HEADER */
.header{
background:#ff7a00;display:flex;align-items:center;
padding:15px 30px;gap:20px;position:sticky;top:0;z-index:1000
}
.logo{color:#fff;font-weight:700;font-size:22px}
.search{flex:1}
.search input{
width:100%;padding:12px 20px;border-radius:40px;border:none
}
.icons{color:#fff;display:flex;gap:20px;cursor:pointer}

/* NAV */
.nav{
background:#fff;padding:12px 30px;
display:flex;gap:25px;border-bottom:1px solid #ddd
}

/* LAYOUT */
.container{
display:grid;grid-template-columns:260px 1fr;
gap:20px;padding:20px
}

/* SIDEBAR */
.sidebar{
background:#fff;padding:15px;border-radius:10px;
box-shadow:0 2px 5px rgba(0,0,0,0.05)
}
.sidebar div{
padding:10px;border-radius:6px;cursor:pointer;
transition:.2s
}
.sidebar div:hover{background:#f2f2f2}

/* HERO SLIDER */
.hero{position:relative;height:320px;border-radius:12px;overflow:hidden}
.slide{
position:absolute;width:100%;height:100%;
opacity:0;transition:.6s
}
.slide.active{opacity:1}
.slide img{width:100%;height:100%;object-fit:cover}

.dots{
position:absolute;bottom:10px;left:50%;
transform:translateX(-50%);
display:flex;gap:6px
}
.dot{
width:10px;height:10px;background:#fff;
border-radius:50%;opacity:.5;cursor:pointer
}
.dot.active{opacity:1}

/* PRODUCTS */
.products{
display:grid;
grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
gap:20px;margin-top:30px
}
.card{
background:#fff;padding:15px;border-radius:10px;
transition:.3s
}
.card:hover{
transform:translateY(-6px);
box-shadow:0 10px 20px rgba(0,0,0,0.1)
}
.card img{width:100%;height:200px;object-fit:cover}
.btn{
background:#0a6cdc;color:#fff;padding:10px;
border:none;margin-top:10px;cursor:pointer;border-radius:5px
}

/* CART */
.cart{
position:fixed;right:-350px;top:0;
width:300px;height:100%;background:#fff;
box-shadow:-2px 0 10px rgba(0,0,0,0.1);
padding:20px;transition:.3s;z-index:2000
}
.cart.open{right:0}

/* MOBILE */
@media(max-width:768px){
.container{grid-template-columns:1fr}
.sidebar{display:none}
}
</style>
</head>

<body>

<div class="topbar">
<span>🔥 Fast Delivery | Best Prices | Trusted Store 🔥</span>
</div>

<div class="header">
<div class="logo">ABC Store</div>

<div class="search">
<input placeholder="Search products">
</div>

<div class="icons" onclick="toggleCart()">🛒 (<span id="count">0</span>)</div>
</div>

<div class="nav">
Home Hot Deals Solar UPS Track Order
</div>

<div class="container">

<div class="sidebar">
<h3>Categories</h3>
<div>Solar</div>
<div>Inverters</div>
<div>Batteries</div>
</div>

<div>

<!-- SLIDER -->
<div class="hero">
<div class="slide active"><img src="https://via.placeholder.com/800x400"></div>
<div class="slide"><img src="https://via.placeholder.com/800x400/ff7a00"></div>
<div class="slide"><img src="https://via.placeholder.com/800x400/333"></div>

<div class="dots"></div>
</div>

<div class="products" id="products"></div>

</div>

</div>

<!-- CART -->
<div class="cart" id="cart">
<h3>Cart</h3>
<div id="cart-items"></div>
<p>Total: Rs. <span id="total">0</span></p>
</div>

<script>
const products=[
{id:1,name:"Product 1",price:1000,img:"https://via.placeholder.com/300"},
{id:2,name:"Product 2",price:1500,img:"https://via.placeholder.com/300"},
{id:3,name:"Product 3",price:2000,img:"https://via.placeholder.com/300"},
{id:4,name:"Product 4",price:2500,img:"https://via.placeholder.com/300"}
];

let cart=JSON.parse(localStorage.getItem("cart"))||[];

// render products
document.getElementById("products").innerHTML=
products.map(p=>`
<div class="card">
<img src="${p.img}">
<h4>${p.name}</h4>
<p>Rs. ${p.price}</p>
<button class="btn" onclick="addToCart(${p.id})">Add</button>
</div>
`).join("");

// cart logic
function addToCart(id){
let item=cart.find(p=>p.id===id);
if(item){item.qty++}
else{
let p=products.find(p=>p.id===id);
cart.push({...p,qty:1});
}
updateCart();
}

function updateCart(){
localStorage.setItem("cart",JSON.stringify(cart));
document.getElementById("count").innerText=
cart.reduce((a,b)=>a+b.qty,0);

document.getElementById("cart-items").innerHTML=
cart.map(i=>`<p>${i.name} x${i.qty}</p>`).join("");

document.getElementById("total").innerText=
cart.reduce((a,b)=>a+(b.price*b.qty),0);
}

function toggleCart(){
document.getElementById("cart").classList.toggle("open");
}

// slider
let slides=document.querySelectorAll(".slide");
let dotsContainer=document.querySelector(".dots");
let index=0;

slides.forEach((_,i)=>{
let d=document.createElement("div");
d.className="dot";
d.onclick=()=>showSlide(i);
dotsContainer.appendChild(d);
});

function showSlide(i){
slides.forEach(s=>s.classList.remove("active"));
dotsContainer.children[index].classList.remove("active");

index=i;
slides[index].classList.add("active");
dotsContainer.children[index].classList.add("active");
}

setInterval(()=>{
showSlide((index+1)%slides.length);
},3000);

updateCart();
</script>

</body>
</html>
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ABC Store | Premium Ecommerce</title>
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700;800&display=swap" rel="stylesheet">
<style>
:root{
  --orange:#ff7a00;
  --orange-dark:#e76a00;
  --dark:#111111;
  --text:#1f2937;
  --muted:#6b7280;
  --bg:#f5f7fb;
  --white:#ffffff;
  --border:#e5e7eb;
  --shadow:0 14px 30px rgba(17, 24, 39, 0.08);
}

*{box-sizing:border-box}
html,body{margin:0;padding:0}
body{
  font-family:'DM Sans',sans-serif;
  color:var(--text);
  background:var(--bg);
  line-height:1.4;
}

a{color:inherit;text-decoration:none}
button,input{font-family:inherit}

/* ---------- TOP ANNOUNCEMENT BAR ---------- */
.topbar{
  background:var(--dark);
  color:var(--white);
  font-size:13px;
  font-weight:500;
  overflow:hidden;
  white-space:nowrap;
  padding:8px 0;
}
.topbar-track{
  display:inline-flex;
  min-width:100%;
  gap:56px;
  animation:marquee 22s linear infinite;
}
.topbar-track span{padding-left:24px}
@keyframes marquee{
  0%{transform:translateX(0)}
  100%{transform:translateX(-50%)}
}

/* ---------- PREMIUM STICKY HEADER ---------- */
.header-wrap{
  position:sticky;
  top:0;
  z-index:3000;
  box-shadow:0 6px 18px rgba(0,0,0,.08);
}
.header{
  background:var(--orange);
  color:var(--white);
  padding:14px 26px;
  display:grid;
  grid-template-columns:auto 1fr auto;
  gap:18px;
  align-items:center;
}
.logo{
  font-size:1.5rem;
  font-weight:800;
  letter-spacing:.3px;
}
.search{
  position:relative;
}
.search input{
  width:100%;
  border:none;
  border-radius:999px;
  padding:13px 48px 13px 18px;
  background:rgba(255,255,255,.95);
  color:var(--text);
  outline:none;
  font-size:15px;
}
.search .search-icon{
  position:absolute;
  right:16px;
  top:50%;
  transform:translateY(-50%);
  color:#555;
  pointer-events:none;
}
.header-actions{
  display:flex;
  gap:10px;
  align-items:center;
}
.icon-btn{
  border:1px solid rgba(255,255,255,.45);
  color:var(--white);
  background:rgba(255,255,255,.14);
  border-radius:999px;
  height:42px;
  min-width:42px;
  padding:0 14px;
  display:inline-flex;
  align-items:center;
  justify-content:center;
  gap:6px;
  cursor:pointer;
  transition:.25s ease;
}
.icon-btn:hover{background:rgba(255,255,255,.25)}
.cart-count{
  display:inline-flex;
  align-items:center;
  justify-content:center;
  min-width:20px;
  height:20px;
  border-radius:999px;
  background:var(--dark);
  font-size:12px;
  font-weight:700;
}

/* ---------- NAVIGATION ---------- */
.nav{
  background:var(--white);
  border-bottom:1px solid var(--border);
  padding:0 20px;
}
.nav ul{
  max-width:1280px;
  margin:0 auto;
  list-style:none;
  padding:0;
  display:flex;
  gap:8px;
  flex-wrap:wrap;
}
.nav a{
  display:block;
  padding:14px 14px;
  font-weight:600;
  color:#374151;
  border-radius:8px;
  transition:.25s ease;
}
.nav a:hover{background:#fff2e6;color:var(--orange)}

/* ---------- MAIN LAYOUT ---------- */
.page{
  max-width:1280px;
  margin:20px auto 40px;
  padding:0 16px;
  display:grid;
  grid-template-columns:260px 1fr;
  gap:20px;
}
.sidebar,
.surface{
  background:var(--white);
  border-radius:14px;
  box-shadow:var(--shadow);
}

/* ---------- SIDEBAR ---------- */
.sidebar{padding:16px 14px;height:fit-content}
.sidebar h3{margin:4px 10px 14px;font-size:1.06rem}
.category-list{display:grid;gap:8px}
.category-item{
  padding:10px 12px;
  border-radius:10px;
  cursor:pointer;
  transition:.2s ease;
  font-weight:500;
}
.category-item:hover{
  background:#fff2e6;
  color:var(--orange-dark);
  transform:translateX(4px);
}

/* ---------- HERO SLIDER ---------- */
.hero{
  position:relative;
  border-radius:14px;
  overflow:hidden;
  min-height:360px;
  box-shadow:var(--shadow);
}
.slide{
  position:absolute;
  inset:0;
  opacity:0;
  transform:scale(1.02);
  transition:opacity .7s ease, transform .7s ease;
}
.slide.active{opacity:1;transform:scale(1)}
.slide img{width:100%;height:100%;object-fit:cover}
.slide-overlay{
  position:absolute;
  inset:0;
  background:linear-gradient(90deg, rgba(0,0,0,.66), rgba(0,0,0,.2));
  display:flex;
  flex-direction:column;
  justify-content:center;
  gap:12px;
  padding:36px;
  color:var(--white);
}
.slide-overlay h2{margin:0;font-size:clamp(1.35rem,4vw,2.25rem)}
.slide-overlay p{margin:0;max-width:470px;color:#e5e7eb}
.cta{
  align-self:flex-start;
  margin-top:8px;
  border:none;
  background:var(--orange);
  color:#fff;
  padding:11px 18px;
  border-radius:9px;
  font-weight:700;
  cursor:pointer;
  transition:.25s;
}
.cta:hover{background:var(--orange-dark);transform:translateY(-1px)}
.slider-controls{
  position:absolute;
  inset-inline:0;
  top:50%;
  transform:translateY(-50%);
  display:flex;
  justify-content:space-between;
  padding:0 12px;
  pointer-events:none;
}
.ctrl{
  pointer-events:auto;
  width:38px;
  height:38px;
  border:none;
  border-radius:50%;
  background:rgba(255,255,255,.85);
  cursor:pointer;
}
.dots{
  position:absolute;
  bottom:12px;
  left:50%;
  transform:translateX(-50%);
  display:flex;
  gap:8px;
}
.dot{
  width:10px;
  height:10px;
  border-radius:50%;
  background:rgba(255,255,255,.5);
  border:1px solid rgba(255,255,255,.7);
  cursor:pointer;
}
.dot.active{background:#fff}

/* ---------- MULTI-BANNER ---------- */
.multi-banner{
  margin-top:20px;
  display:grid;
  gap:16px;
  grid-template-columns:2fr 1.35fr;
}
.banner-large,
.banner-small{
  border-radius:14px;
  overflow:hidden;
  position:relative;
  min-height:160px;
  box-shadow:var(--shadow);
}
.banner-large img,
.banner-small img{width:100%;height:100%;object-fit:cover;display:block}
.banner-caption{
  position:absolute;
  inset:auto 0 0 0;
  padding:14px;
  color:#fff;
  background:linear-gradient(180deg, transparent, rgba(0,0,0,.66));
  font-weight:700;
}
.banner-grid{display:grid;grid-template-columns:1fr 1fr;gap:16px}

/* ---------- PRODUCTS ---------- */
.section-header{
  margin:28px 0 14px;
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:14px;
}
.section-header h3{margin:0;font-size:1.35rem}
.section-header p{margin:0;color:var(--muted)}
.products{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(220px,1fr));
  gap:18px;
}
.card{
  background:#fff;
  border:1px solid var(--border);
  border-radius:14px;
  overflow:hidden;
  transition:transform .25s, box-shadow .25s;
}
.card:hover{transform:translateY(-6px);box-shadow:var(--shadow)}
.card img{width:100%;height:210px;object-fit:cover;display:block}
.card-body{padding:14px}
.card h4{margin:0 0 8px;font-size:1.02rem}
.price{font-weight:800;color:#0f172a}
.btn{
  width:100%;
  margin-top:12px;
  border:none;
  border-radius:10px;
  padding:10px 12px;
  background:var(--orange);
  color:#fff;
  font-weight:700;
  cursor:pointer;
  transition:.2s;
}
.btn:hover{background:var(--orange-dark)}

/* ---------- CART SIDEBAR ---------- */
.backdrop{
  position:fixed;
  inset:0;
  background:rgba(15,23,42,.45);
  opacity:0;
  pointer-events:none;
  transition:.3s;
  z-index:3500;
}
.backdrop.open{opacity:1;pointer-events:auto}
.cart{
  position:fixed;
  top:0;
  right:0;
  width:min(420px,100%);
  height:100vh;
  background:#fff;
  box-shadow:-6px 0 24px rgba(0,0,0,.16);
  transform:translateX(100%);
  transition:transform .34s ease;
  z-index:4000;
  display:flex;
  flex-direction:column;
}
.cart.open{transform:translateX(0)}
.cart-head{
  padding:16px;
  border-bottom:1px solid var(--border);
  display:flex;
  justify-content:space-between;
  align-items:center;
}
.cart-items{padding:14px;overflow:auto;flex:1;display:grid;gap:10px}
.cart-item{
  border:1px solid var(--border);
  border-radius:10px;
  padding:10px;
  display:grid;
  grid-template-columns:64px 1fr;
  gap:10px;
}
.cart-item img{width:64px;height:64px;border-radius:8px;object-fit:cover}
.cart-item h5{margin:0 0 4px;font-size:14px}
.qty-row{display:flex;align-items:center;gap:8px;margin-top:6px}
.qty-btn,
.remove-btn{
  border:1px solid var(--border);
  background:#fff;
  border-radius:7px;
  cursor:pointer;
  padding:4px 8px;
}
.remove-btn{color:#b91c1c}
.cart-foot{
  border-top:1px solid var(--border);
  padding:14px;
  display:grid;
  gap:10px;
}
.total-line{display:flex;justify-content:space-between;font-weight:800;font-size:1.05rem}
.checkout{
  border:none;
  border-radius:10px;
  padding:11px;
  background:var(--orange);
  color:#fff;
  font-weight:700;
  cursor:pointer;
}
.empty{color:var(--muted);text-align:center;padding:30px 12px}

/* ---------- RESPONSIVE ---------- */
@media (max-width:1024px){
  .page{grid-template-columns:1fr}
  .sidebar{display:none}
  .multi-banner{grid-template-columns:1fr}
}
@media (max-width:768px){
  .header{grid-template-columns:1fr;gap:10px;padding:12px}
  .header-actions{justify-content:flex-end}
  .hero{min-height:300px}
  .slide-overlay{padding:20px}
  .banner-grid{grid-template-columns:1fr}
  .nav{padding:0 10px}
  .nav ul{gap:2px}
  .nav a{padding:11px 10px;font-size:14px}
}
</style>
</head>
<body>

<!-- Top announcement bar -->
<div class="topbar">
  <div class="topbar-track">
    <span>🔥 Free shipping above Rs. 3,000</span>
    <span>⚡ Same-day dispatch for Karachi orders</span>
    <span>✅ 100% original products with warranty</span>
    <span>🎁 New member discount: SAVE10</span>
    <span>🔥 Free shipping above Rs. 3,000</span>
    <span>⚡ Same-day dispatch for Karachi orders</span>
  </div>
</div>

<div class="header-wrap">
  <!-- Premium header -->
  <header class="header">
    <div class="logo">ABC Store</div>

    <label class="search">
      <input type="search" placeholder="Search for inverters, batteries, solar kits..." aria-label="Search products">
      <span class="search-icon">🔍</span>
    </label>

    <div class="header-actions">
      <button class="icon-btn" type="button" aria-label="User account">👤</button>
      <button class="icon-btn" type="button" onclick="toggleCart(true)" aria-label="Open cart">
        🛒 <span class="cart-count" id="count">0</span>
      </button>
    </div>
  </header>

  <!-- Navigation -->
  <nav class="nav" aria-label="Main navigation">
    <ul>
      <li><a href="#">Home</a></li>
      <li><a href="#">Shop</a></li>
      <li><a href="#">Hot Deals</a></li>
      <li><a href="#">Solar Solutions</a></li>
      <li><a href="#">UPS & Inverters</a></li>
      <li><a href="#">Track Order</a></li>
      <li><a href="#">Contact</a></li>
    </ul>
  </nav>
</div>

<main class="page">
  <!-- Left Sidebar -->
  <aside class="sidebar">
    <h3>Shop by Categories</h3>
    <div class="category-list">
      <div class="category-item">Solar Panels</div>
      <div class="category-item">UPS Inverters</div>
      <div class="category-item">Batteries</div>
      <div class="category-item">Power Stations</div>
      <div class="category-item">Controllers</div>
      <div class="category-item">Generators</div>
      <div class="category-item">Accessories</div>
    </div>
  </aside>

  <section>
    <!-- Hero Slider -->
    <div class="hero" id="heroSlider">
      <article class="slide active">
        <img src="https://images.unsplash.com/photo-1473341304170-971dccb5ac1e?auto=format&fit=crop&w=1400&q=80" alt="Premium solar solutions">
        <div class="slide-overlay">
          <h2>Power Your Home with Premium Solar Kits</h2>
          <p>Build a smarter, cleaner home energy setup with top-tier brands and trusted installation partners.</p>
          <button class="cta">Shop Solar Deals</button>
        </div>
      </article>
      <article class="slide">
        <img src="https://images.unsplash.com/photo-1577375729152-4c8b5fcda381?auto=format&fit=crop&w=1400&q=80" alt="Advanced inverter systems">
        <div class="slide-overlay">
          <h2>Heavy-Duty Inverters for Uninterrupted Power</h2>
          <p>From apartments to industrial spaces, discover inverter systems engineered for real-world reliability.</p>
          <button class="cta">Explore Inverters</button>
        </div>
      </article>
      <article class="slide">
        <img src="https://images.unsplash.com/photo-1565043666747-69f6646db940?auto=format&fit=crop&w=1400&q=80" alt="Battery storage products">
        <div class="slide-overlay">
          <h2>Long-Life Battery Storage Collection</h2>
          <p>Get optimized backup with fast-charging batteries designed for daily performance and long warranty cycles.</p>
          <button class="cta">View Batteries</button>
        </div>
      </article>

      <div class="slider-controls">
        <button class="ctrl" id="prevSlide" aria-label="Previous slide">‹</button>
        <button class="ctrl" id="nextSlide" aria-label="Next slide">›</button>
      </div>
      <div class="dots" id="dots"></div>
    </div>

    <!-- Multi-banner section -->
    <section class="multi-banner" aria-label="Promotional banners">
      <article class="banner-large">
        <img src="https://images.unsplash.com/photo-1492144534655-ae79c964c9d7?auto=format&fit=crop&w=1200&q=80" alt="Mega sale banner">
        <div class="banner-caption">Mega Weekend Sale • Up to 30% OFF</div>
      </article>

      <div class="banner-grid">
        <article class="banner-small">
          <img src="https://images.unsplash.com/photo-1460353581641-37baddab0fa2?auto=format&fit=crop&w=800&q=80" alt="Smart battery deals">
          <div class="banner-caption">Smart Batteries</div>
        </article>
        <article class="banner-small">
          <img src="https://images.unsplash.com/photo-1517142089942-ba376ce32a2e?auto=format&fit=crop&w=800&q=80" alt="Inverter discounts">
          <div class="banner-caption">Inverter Discounts</div>
        </article>
        <article class="banner-small">
          <img src="https://images.unsplash.com/photo-1581093588401-16ecf2c03f7f?auto=format&fit=crop&w=800&q=80" alt="Portable power">
          <div class="banner-caption">Portable Power</div>
        </article>
        <article class="banner-small">
          <img src="https://images.unsplash.com/photo-1625766763788-54d58f8e4740?auto=format&fit=crop&w=800&q=80" alt="Home energy kits">
          <div class="banner-caption">Home Energy Kits</div>
        </article>
      </div>
    </section>

    <!-- Product Section -->
    <div class="section-header">
      <h3>Featured Products</h3>
      <p>Carefully selected best sellers</p>
    </div>
    <div class="products" id="products"></div>
  </section>
</main>

<!-- Cart UI -->
<div class="backdrop" id="backdrop" onclick="toggleCart(false)"></div>
<aside class="cart" id="cart" aria-label="Shopping cart">
  <div class="cart-head">
    <h3 style="margin:0">Your Cart</h3>
    <button class="qty-btn" type="button" onclick="toggleCart(false)">Close ✕</button>
  </div>

  <div class="cart-items" id="cart-items"></div>

  <div class="cart-foot">
    <div class="total-line">
      <span>Total</span>
      <span>Rs. <span id="total">0</span></span>
    </div>
    <button class="checkout" type="button">Proceed to Checkout</button>
  </div>
</aside>

<script>
/* ---------- Product Data ---------- */
const products = [
  {id:1,name:"Inverex Nitrox 6KW Hybrid Inverter",price:239000,img:"https://images.unsplash.com/photo-1617806118233-18e1de247200?auto=format&fit=crop&w=800&q=80"},
  {id:2,name:"AGS 200Ah Deep Cycle Battery",price:67500,img:"https://images.unsplash.com/photo-1625246333195-78d73c8f0fba?auto=format&fit=crop&w=800&q=80"},
  {id:3,name:"JA Solar 580W Panel (A Grade)",price:40500,img:"https://images.unsplash.com/photo-1509391366360-2e959784a276?auto=format&fit=crop&w=800&q=80"},
  {id:4,name:"Crown Micro UPS 1500VA",price:42500,img:"https://images.unsplash.com/photo-1584277261846-c6a1672ed979?auto=format&fit=crop&w=800&q=80"},
  {id:5,name:"Portable Power Station 1200W",price:158000,img:"https://images.unsplash.com/photo-1580894908361-967195033215?auto=format&fit=crop&w=800&q=80"},
  {id:6,name:"Solar Charge Controller MPPT 80A",price:38900,img:"https://images.unsplash.com/photo-1544723795-3fb6469f5b39?auto=format&fit=crop&w=800&q=80"},
  {id:7,name:"Lithium Battery Pack 5.12kWh",price:320000,img:"https://images.unsplash.com/photo-1603732551687-08d86f6a68ce?auto=format&fit=crop&w=800&q=80"},
  {id:8,name:"Smart Energy Monitor Wi-Fi",price:15900,img:"https://images.unsplash.com/photo-1558002038-1055907df827?auto=format&fit=crop&w=800&q=80"}
];

let cart = JSON.parse(localStorage.getItem("cart")) || [];

/* ---------- Product Rendering ---------- */
function formatPrice(value){
  return value.toLocaleString("en-PK");
}

function renderProducts(){
  document.getElementById("products").innerHTML = products.map(product => `
    <article class="card">
      <img src="${product.img}" alt="${product.name}">
      <div class="card-body">
        <h4>${product.name}</h4>
        <div class="price">Rs. ${formatPrice(product.price)}</div>
        <button class="btn" type="button" onclick="addToCart(${product.id})">Add to Cart</button>
      </div>
    </article>
  `).join("");
}

/* ---------- Cart Logic ---------- */
function saveCart(){
  localStorage.setItem("cart", JSON.stringify(cart));
}

function addToCart(id){
  const existing = cart.find(item => item.id === id);
  if(existing){
    existing.qty += 1;
  } else {
    const product = products.find(item => item.id === id);
    cart.push({...product, qty:1});
  }
  updateCart();
  toggleCart(true);
}

function decreaseQty(id){
  const item = cart.find(product => product.id === id);
  if(!item) return;
  item.qty -= 1;
  if(item.qty <= 0){
    cart = cart.filter(product => product.id !== id);
  }
  updateCart();
}

function increaseQty(id){
  const item = cart.find(product => product.id === id);
  if(!item) return;
  item.qty += 1;
  updateCart();
}

function removeItem(id){
  cart = cart.filter(product => product.id !== id);
  updateCart();
}

function updateCart(){
  saveCart();

  const cartCount = cart.reduce((sum, item) => sum + item.qty, 0);
  document.getElementById("count").textContent = cartCount;

  const cartItems = document.getElementById("cart-items");
  if(cart.length === 0){
    cartItems.innerHTML = '<p class="empty">Your cart is empty. Start shopping now ✨</p>';
  } else {
    cartItems.innerHTML = cart.map(item => `
      <article class="cart-item">
        <img src="${item.img}" alt="${item.name}">
        <div>
          <h5>${item.name}</h5>
          <div>Rs. ${formatPrice(item.price)}</div>
          <div class="qty-row">
            <button class="qty-btn" type="button" onclick="decreaseQty(${item.id})">−</button>
            <strong>${item.qty}</strong>
            <button class="qty-btn" type="button" onclick="increaseQty(${item.id})">+</button>
            <button class="remove-btn" type="button" onclick="removeItem(${item.id})">Remove</button>
          </div>
        </div>
      </article>
    `).join("");
  }

  const total = cart.reduce((sum, item) => sum + (item.price * item.qty), 0);
  document.getElementById("total").textContent = formatPrice(total);
}

function toggleCart(forceOpen){
  const cartSidebar = document.getElementById("cart");
  const backdrop = document.getElementById("backdrop");
  const shouldOpen = typeof forceOpen === "boolean" ? forceOpen : !cartSidebar.classList.contains("open");

  cartSidebar.classList.toggle("open", shouldOpen);
  backdrop.classList.toggle("open", shouldOpen);
}

/* ---------- Hero Slider ---------- */
const slides = document.querySelectorAll(".slide");
const dotsContainer = document.getElementById("dots");
const prevBtn = document.getElementById("prevSlide");
const nextBtn = document.getElementById("nextSlide");
let currentSlide = 0;
let autoPlay;

function createDots(){
  slides.forEach((_, i) => {
    const dot = document.createElement("button");
    dot.className = "dot";
    dot.type = "button";
    dot.setAttribute("aria-label", `Go to slide ${i + 1}`);
    dot.addEventListener("click", () => showSlide(i));
    dotsContainer.appendChild(dot);
  });
}

function showSlide(index){
  slides[currentSlide].classList.remove("active");
  dotsContainer.children[currentSlide].classList.remove("active");
  currentSlide = (index + slides.length) % slides.length;
  slides[currentSlide].classList.add("active");
  dotsContainer.children[currentSlide].classList.add("active");
}

function startAutoPlay(){
  clearInterval(autoPlay);
  autoPlay = setInterval(() => showSlide(currentSlide + 1), 4200);
}

prevBtn.addEventListener("click", () => {
  showSlide(currentSlide - 1);
  startAutoPlay();
});

nextBtn.addEventListener("click", () => {
  showSlide(currentSlide + 1);
  startAutoPlay();
});

createDots();
showSlide(0);
startAutoPlay();
renderProducts();
updateCart();
</script>
</body>
</html>